{
  "name": "<%= @application_name %>",
  "cwd": "<%= @cwd %>",
  "script": "<%= @script %>",
  "env": {
    <%= @environment.to_a.map { |pair| pair.map { |x| '"' + x + '"' }.join(': ') }.join(",\n") %>
  }
}
