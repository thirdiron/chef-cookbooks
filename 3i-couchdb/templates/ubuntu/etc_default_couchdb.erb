export ERL_MAX_PORTS=<%= @max_connections %>

# According to https://wiki.apache.org/couchdb/Performance
# The following adjustment tells erlang to use multiple
# threads and is helpful for keeping couchdb responsive
# during periods of heavy disk utilization
export ERL_FLAGS="+A <%= @num_erlang_threads %>"
